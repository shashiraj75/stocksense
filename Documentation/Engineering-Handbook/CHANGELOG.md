# CHANGELOG

## Unreleased

### Added

- **Phase 1A.6 — Market Integrity Hardening: production deployment and database-default closure (2026-07-14).** Commit `36d4b33` deployed to Railway and verified live. `log_prediction`/`log_outcome` (both backends) require an explicit, validated `market`; manifest schema version 2 is live with version-1 manifests deprecated and fail-closed; the repair-plan validator's forbidden-field check is now recursive and depth-bounded. Separately, the production database-level `DEFAULT 'IN'` on `predictions.market`/`outcomes.market` was dropped (2026-07-14T05:54:02Z) under an explicitly authorized, fully evidenced migration — both columns remain `NOT NULL`, pre/post row counts are identical, zero historical rows were changed. No historical contamination repair, backfill, or relabeling has been executed; none is authorized. Production learning remains quarantined. See [Phase 1A.6](../Architecture/Phase-1A6-Market-Integrity-Hardening-and-Repair-Planning.md) and [Product Integrity #003](Releases/Product-Integrity-003-Phase-1A6-Production-Migration-and-Natural-Run-Verification.md).
- **India and US Daily Picks natural-run completion evidence (2026-07-14).** Both markets' scheduled generation completed successfully on the current deployed code (Sprint #014's stratified universe + Phase 1A.6) — the first live, natural (scheduler-fired) confirmation this pipeline completes end-to-end for either market since Sprint #014 shipped. A separate, older, interrupted US job (`aa73f80b`, 2026-07-13) was root-caused, confirmed unrelated to Phase 1A.6, and confirmed inert/non-blocking — no cleanup required. See [Product Integrity #003](Releases/Product-Integrity-003-Phase-1A6-Production-Migration-and-Natural-Run-Verification.md).
- **Progressive per-horizon outcome resolution (`6b166f8`, 2026-07-13).** The outcome resolver can now resolve short/medium/long-horizon returns independently as each horizon's own forward window matures, instead of requiring all three together; already-resolved horizons are preserved, unresolved ones are simply left open for a later pass. A live-scheduling/write-timing behavior, distinct from and predating Phase 1A.6's market-conflict containment and the separate deterministic manifest-backfill tooling — no historical backfill was executed. See [Phase 1A.6 §10](../Architecture/Phase-1A6-Market-Integrity-Hardening-and-Repair-Planning.md#10-resolver-conflict-exclusion).

- **Sprint #014 — Daily Picks Large/Mid/Small-Cap Stratification and Confidence Priority.** Replaced Yahoo-screener-based universe discovery in `daily_picks.py` with the already-existing, nightly-refreshed `stock_fundamentals_cache` (screener.in/yfinance-sourced, previously unused by Daily Picks). Universe was structurally Large+Mid cap only in both markets (India's old 250-symbol cutoff, US's old $2,000M floor) with a further 50-stock momentum-narrowed deep-scoring pool shared across all horizons. Now stratifies a ~400-symbol pool ~40/30/30 across Large/Mid/Small (SEBI rank convention for India, value convention for US). Phase 5 selection differs by horizon: short-term prioritizes >80% confidence with fill-down (never padded with noise on a weak-conviction day); medium/long-term enforces a 2/2/2 tier quota. 21 new/updated tests, 1517/1517 suite passing; two obsolete Yahoo-screener-retry test files deleted. Not yet observed in a live production run. See [Sprint #014](Releases/Sprint-014-Daily-Picks-Cap-Stratification-and-Confidence-Priority.md).
- **Sprint #013 — Recommendation Consolidation Frontend Test Foundation.** Added a frontend test framework (Vitest + React Testing Library + jsdom), which did not exist at all before this sprint, plus 32 tests covering the RCI/Research-Report contract-validation boundary (`getValidRecommendationConsolidation`/`getValidResearchReport`) and the `DisclosurePanel`/`EvidenceSummary` components. New `frontend_tests.yml` GitHub Actions workflow runs the suite plus `tsc --noEmit` on every `frontend/**` push/PR, mirroring `backend_tests.yml`. Closes the exact gap Sprint #012 disclosed (validated only via manual script execution, no committed suite) and acts on `MASTER-ROADMAP.md`'s own recommendation. No production code, contract, or UI changed; `RCI_LIVE_STOCK_ANALYSIS_ENABLED` remains disabled. See [Sprint #013](Releases/Sprint-013-Recommendation-Consolidation-Frontend-Test-Foundation.md).

### Fixed

- **US Premarket Finalizer missing its window entirely (2026-07-13):** live-observed via the GitHub Actions UI — the scheduled cron for `daily_picks_us_premarket.yml` recorded 0 runs on 2026-07-10 despite both DST-candidate trigger times having passed, meaning `finalize_premarket()`'s original 30-minute acceptance window (8:00-8:30 AM ET, in `premarket_finalizer.py`'s `in_premarket_window`) closed before GitHub Actions' own scheduling delay let the trigger land. Moved the cron 30 minutes earlier (7:35 AM ET-equivalent, both DST candidates) and widened the backend's acceptance window from 30 to 90 minutes (7:30-9:00 AM ET) to absorb that class of delay without drifting into regular-session data. Widening past the 1-hour EDT/EST gap between the two cron candidates means both now land inside the window on the same day — added a same-day idempotency guard (`premarket_finalized_at` check) to `finalize_premarket()` so the second candidate safely no-ops instead of duplicating the day's finalization. 5 tests updated/added, sanity-checked per SES-003 §4 (deliberately disabled the guard, confirmed the corresponding tests failed, restored). Monitoring the next real Monday US market-day run to confirm the fix actually lands the trigger inside the new window.
- **Multibagger (all India screens returned zero results):** `promoter_pledge_pct` is `NULL` (not `0`) for any stock screener.in shows no pledge disclosure for, since `screener_data.py` only sets it when a "Pledge" row exists on the page. Every India screen's `promoter_pledge_pct < N` condition failed on that `NULL`, excluding almost every clean company. `backend/services/fundamentals_cache.py`'s `_SCREENS` now use `COALESCE(promoter_pledge_pct, 0) < N`. Verified live: `quality_compounder` 0→52, `multibagger_discovery` 0→167, `tenbagger_early` 0→73.
- **Portfolio:** added a Day's P&L column (amount + %), a By Sector/By Stock allocation toggle (`PortfolioAllocationChart.tsx`), fixed India vs. US allocation-view inconsistency (sector-data threshold and a stale-default `useState` bug), fixed real mobile table overflow, fixed wrapped P&L number spacing and an empty Signal column.
- **Stock Detail:** AI Signal panel and Daily Picks confidence bars now tier BUY color by confidence (mirroring `SignalBadge`) instead of showing flat green regardless of strength; opening a stock from a specific Daily Picks horizon now lands on that same horizon tab instead of always Short Term; fixed the History tab's Factor Breakdown chart rendering completely empty (Recharts Fragment-vs-array children scanning + a stuck-animation `requestAnimationFrame` dependency, both in `ScoreHistoryChart.tsx`).

### Documentation

- Added the Current Release Status register as the authoritative source for live release state, validation gates, feature flags, scheduler state, and operational blockers.
- Reconciled roadmap and architecture references so Recommendation Consolidation Intelligence (Epic 005) is distinct from future Prediction and Recommendation Decision Architecture Evolution (Epic 006).
- Marked the legacy Selection Engine roadmap as historical rather than current implementation guidance.
- Recorded Session 10 (2026-07-10) fixes in `Documentation/STOCKSENSE_DOCUMENTATION.md` §27 Changelog. Flagged that the Multibagger Screen had no dedicated numbered section in that document (only glossary mentions in §18a) — **resolved same day**: added §18b Multibagger Screen (the three screens' actual SQL criteria per market, the scorecard/verdict/red-flag/elite_strong_buy logic, the Session 10 pledge-NULL fix, and frontend behavior).
